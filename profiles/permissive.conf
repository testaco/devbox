# Devbox Egress Profile: Permissive
# All outbound network access allowed (current default behavior)

# Profile metadata
PROFILE_NAME="permissive"
PROFILE_DESCRIPTION="All egress allowed - no network restrictions"

# Network mode: bridge (default), none, or custom
NETWORK_MODE="bridge"

# Default action: accept or drop
DEFAULT_ACTION="accept"

# Logging: true or false
LOG_BLOCKED="false"
LOG_ALL="false"

# No filtering - all traffic allowed
ALLOWED_PORTS=""
ALLOWED_DOMAINS=""
BLOCKED_DOMAINS=""
ALLOWED_IPS=""
BLOCKED_IPS=""
