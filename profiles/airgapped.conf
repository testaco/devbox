# Devbox Egress Profile: Airgapped
# No network connectivity - fully isolated

# Profile metadata
PROFILE_NAME="airgapped"
PROFILE_DESCRIPTION="No network access - fully isolated container"

# Network mode: none = Docker's --network none
NETWORK_MODE="none"

# Default action: drop (though network=none handles this)
DEFAULT_ACTION="drop"

# Logging: not applicable with network=none
LOG_BLOCKED="false"
LOG_ALL="false"

# No network access at all
ALLOWED_PORTS=""
ALLOWED_DOMAINS=""
BLOCKED_DOMAINS=""
ALLOWED_IPS=""
BLOCKED_IPS=""
